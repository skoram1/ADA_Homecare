SELECT
  DAY_DATE,
  SOP_FLAG,
  ADD_MONTHS(MTH_START_DATE, 1) AS MTH_START_DATE,
  LAST_DAY(ADD_MONTHS(MTH_START_DATE, 2)) AS MTH_END_DATE
FROM idpdm.md_time_fdim
WHERE SOP_FLAG = 'Y' AND DAY_OF_WK_NAME = 'MONDAY';
